# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# CalDAV and iCalendar
caldav>=1.3.0
icalendar>=5.0.0
recurring-ical-events>=2.1.0

# Database
aiosqlite>=0.19.0
sqlalchemy>=2.0.0

# Validation and settings
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Date handling
python-dateutil>=2.8.0

# HTTP client (for async operations)
httpx>=0.26.0

# Security (for credential storage)
cryptography>=42.0.0

# Build tools (for Electron packaging)
pyinstaller>=6.0.0
